# Synscapes (Unofficial) Dataloader

To read depth maps on Ubuntu, install OpenEXR:

```
sudo apt install openexr
sudo apt install libopenexr-dev
pip3 install openexr  # `sudo apt install python3-pip` if need be
```
